[project]
name = "mcpserver-demo"
version = "0.1.0"
description = "Simple Goal Setting MCP Server for learning FastMCP"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=1.0.0",
]

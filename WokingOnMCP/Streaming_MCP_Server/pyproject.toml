[project]
name = "streaming-mcp-server"
version = "0.1.0"
description = "Demo MCP Server with streaming capabilities"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=1.25.0",
    "langsmith>=0.1.0",
    "python-dotenv>=1.0.0",
]